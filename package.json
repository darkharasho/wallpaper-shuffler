{
  "name": "wallpaper-shuffler",
  "version": "2.1.4",
  "description": "Desktop wallpaper management app with built-in rotation controls for KDE Plasma",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "vite": "vite",
    "build": "vite build",
    "dev": "concurrently -k \"npm run vite\" \"wait-on http://localhost:8844 && cross-env VITE_DEV_SERVER_URL=http://localhost:8844 electron .\"",
    "dev:update": "concurrently -k \"npm run vite\" \"wait-on http://localhost:8844 && cross-env VITE_DEV_SERVER_URL=http://localhost:8844 WALLPAPER_SHUFFLER_FAKE_UPDATES=1 electron .\"",
    "build:linux": "npm run build && node scripts/run-electron-builder.mjs --linux",
    "build:win": "npm run build && node scripts/run-electron-builder.mjs --win",
    "build:github": "node scripts/build-github.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "electron": "^35.0.0",
    "electron-builder": "^25.1.8",
    "vite": "^6.0.0",
    "wait-on": "^8.0.0"
  },
  "dependencies": {
    "electron-updater": "^6.7.3"
  },
  "build": {
    "appId": "com.wallpapershuffler.app",
    "productName": "Wallpaper Shuffler",
    "directories": {
      "output": "dist_out"
    },
    "artifactName": "Wallpaper-Shuffler-${version}-${os}-${arch}.${ext}",
    "publish": {
      "provider": "github",
      "owner": "${env.GITHUB_RELEASE_OWNER}",
      "repo": "${env.GITHUB_RELEASE_REPO}",
      "releaseType": "draft"
    },
    "files": [
      "main.js",
      "preload.js",
      "dist/**/*",
      "public/imgs/logo.png",
      "index.html",
      "renderer.js",
      "style.css",
      "package.json",
      "README.md"
    ],
    "linux": {
      "icon": "public/imgs/logo.png",
      "target": [
        "AppImage"
      ]
    },
    "win": {
      "icon": "public/imgs/logo.png",
      "target": [
        "nsis"
      ]
    }
  }
}
